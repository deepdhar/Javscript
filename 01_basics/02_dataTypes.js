"use strict"; // treat all JS code as newer version

// alert(3+3)   -   we are using nodeJS, not browser, hence will not run

console.log(3 + 3)


let name = "deep"
let age = 22
let isLoggedIn = false
let state = null


// number => 2 to the power of 53 
// bigInt => more than number
// string => "" use double quoted or single quotes
// boolean => true / false
// null => stadalone value
// undefined => value not defined in a declared variable
// symbol => unique


// object

console.log(typeof undefined);